PUBLIC_API_URL=http://localhost:3001
PUBLIC_CLIENT_AUTH=true

# Google AdSense (background display ads — create a Display ad unit in AdSense for the slot ID)
PUBLIC_ADSENSE_CLIENT=ca-pub-2530451454702601
PUBLIC_ADSENSE_SLOT=

# Supabase (client-side; use publishable key, not service role)
PUBLIC_SUPABASE_URL=https://cripdxyzassukwfwkpfy.supabase.co
PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# Auth (required) — generate secret: openssl rand -hex 32
AUTH_SECRET=generate-with-openssl-rand-hex-32

# Google OAuth (optional)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Required for local dev (Vite on :5173)
AUTH_TRUST_HOST=true
